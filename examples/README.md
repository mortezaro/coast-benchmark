# Examples

This folder contains lightweight examples that are safe to commit.

- `results/`: compact V5 result tables with task baselines and model scores.

The repository does not include raw audio examples. Human evaluation packets can be generated locally from source data with the packet builders in `data_generation/`.
